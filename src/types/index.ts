export interface Department {
  id: string;
  name: string;
  email: string;
  phone: string;
  head: string;
}

export interface FAQ {
  question: string;
  answer: string;
}

export interface SocialMedia {
  name: string;
  icon: string;
  url: string;
}
